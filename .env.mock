# Modo sin backend (`npm run dev:mock`). Igual que .env.development pero con
# VITE_USE_MOCKS activado: src/lib/api.ts intercepta las peticiones y las
# responde con datos locales (ver src/mocks/), sin depender de Django.
VITE_API_URL=/api
VITE_ENVIRONMENT=dev
VITE_USE_MOCKS=true
